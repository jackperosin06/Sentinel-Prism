# Copy to .env locally (never commit .env). NFR3: no real secrets in repo.
DATABASE_URL=postgresql+asyncpg://user:pass@localhost:5432/sentinel_prism

# Optional: docker-compose Postgres (Story 1.2+ local dev)
POSTGRES_USER=sentinel
POSTGRES_PASSWORD=change-me-local-only
POSTGRES_DB=sentinel_prism
